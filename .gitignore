# Patterns to ignore when building packages.
# This supports shell glob matching, relative path matching, and
# negation (prefixed with !). Only one pattern per line.
.DS_Store
# Common VCS dirs
.git/
.gitignore
.bzr/
.bzrignore
.hg/
.hgignore
.svn/
# Common backup files
*.swp
*.bak
*.tmp
*.orig
*~
# Various IDEs
.project
.idea/
*.tmproj
.vscode/
# environment files
*.env

# test environment
test/terraform/terraform.tfstate
test/terraform/terraform.tfstate.backup
test/terraform/set-up-local.sh
test/terraform/okteto.yml
.terraform
test/terraform/.terraform.tfstate.lock.info
.cache
.go
go.work
go.work.sum
